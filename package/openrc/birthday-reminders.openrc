#!/sbin/openrc-run

name="birthday-reminders"
description="Birthday Reminders"

command="/usr/bin/birthday-reminders"
command_args="--config /etc/birthday-reminders/config.yaml serve"
command_user="root:root"
command_background=true

pidfile="/run/${RC_SVCNAME}.pid"
directory="/var/lib/birthday-reminders"

depend() {
	need net
	after postgresql
}

start_pre() {
	checkpath --directory --owner root:root /run
}
