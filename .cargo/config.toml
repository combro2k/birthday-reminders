[build]
rustc-wrapper = "/usr/bin/sccache"
# Limit parallel jobs to reduce memory usage (4GB RAM is tight)
jobs = 4

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
